name: CarPlayBrowser
options:
  bundleIdPrefix: com.carplaybrowser
targets:
  CarPlayBrowser:
    type: application
    platform: iOS
    deploymentTarget: "14.0"
    sources:
      - Sources
    info:
      path: Info.plist
    entitlements:
      path: CarPlayBrowser.entitlements
